extends layout 

block content
    h1= title 

    article(class="item-article")
    p(class="item-article__description")= item.description
    p Price: $#{item.price}
    p In Stock: $#{item.qtyInStock}
    p Category: 
        a(href=item.category.url) #{item.category.name}